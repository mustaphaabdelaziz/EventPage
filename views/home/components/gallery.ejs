<section>
  <div class="container-fluid">
    <div class="row">
      <% for(g of gallery) {%>
        <div class="col-lg-3 col-md-4 col-sm-6 col-12 mt-2">
          <img class="bd-placeholder-img card-img-top shadow" src="<%= g.picture.url %>" alt="<%= g.picture.filename %>" />
        </div>
        <% } %>
    </div>
  </div>
</section>